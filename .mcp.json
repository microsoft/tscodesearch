{
  "mcpServers": {
    "tscodesearch": {
      "type": "stdio",
      "command": "${workspaceFolder}/.client-venv/Scripts/python.exe",
      "args": [
        "${workspaceFolder}/mcp_server.py"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}
